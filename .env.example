# Optional: if omitted, frontend uses same-origin /api
VITE_CONTROL_API_BASE_URL=

# Required for protected endpoints in nexus-control
VITE_CONTROL_API_TOKEN=replace-with-your-admin-token

# Dev server proxy target for /api requests
VITE_DEV_PROXY_TARGET=http://127.0.0.1:9091
